
// const matchingRequest = findMatchingRequest(index, "Hire");
// return 

// findMatchingRequest(index, "Hire").requested ? (
// 	<>
// 		{findMatchingRequest(index, "Hire").pending ? (
// 			<button className="" onClick={() => deleteHireRequest(index)}>
// 				Cancel Hire Request
// 			</button>
// 		) : (
// 			<>
// 				{findMatchingRequest(index, "Hire").accepted ? (
// 					<button className="" disabled={true}>
// 						Hire Request Accepted
// 					</button>
// 				) : (
// 					<button className=" disabled-but" disabled={true}>
// 						Hire Requested Declined
// 					</button>
// 				)}
// 			</>
// 		)}
// 	</>
// ) : (
// 	<button className="" onClick={() => sendHireRequest(index)}>
// 		Send Hire Request
// 	</button>
// );
